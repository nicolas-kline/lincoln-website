window.NOVA_CONFIG = {
  // Local default only. In GitHub Pages deploys, this file is overwritten by
  // .github/workflows/deploy-test-site.yml using the OPENAI_API_KEY secret.
  openaiApiKey: '',

  // Optional local model override.
  openaiModel: 'gpt-4.1-mini'
};
